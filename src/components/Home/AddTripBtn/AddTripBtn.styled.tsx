import { Button } from 'components/common';
import styled from 'styled-components';

export const AddTripBtnStyled = styled(Button)`
  min-width: 148px;
  min-height: 148px;
  border-radius: 8px;
  font-size: 16px;

  svg {
    font-size: 28px;
  }
`;
